{
  "name": "tradingpanel",
  "version": "0.1.0",
  "description": "Intraday momentum backtest and dashboard (standalone)",
  "scripts": {
    "preview": "cd app && python3 -m http.server 8001",
    "backtest": "node app/scripts/run_backtest.js",
    "export-results": "node app/scripts/export_results_to_csv.js"
  },
  "engines": {
    "node": ">=18"
  }
}
